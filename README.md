
# learn-react-router
How to build a Single Page Application (SPA) using React Router, where navigating between pages updates the URL without reloading the browser. It starts with the basics and gradually introduces nested layouts, dynamic routes, navigation hooks, and data passing.

# React Router Learning Project 🚀

This project was built while learning **React Router** with React. It demonstrates the fundamentals of client-side routing, nested layouts, navigation, and modern routing techniques using `createBrowserRouter`.

## 📚 Topics Covered

* React Router DOM setup
* BrowserRouter
* createBrowserRouter
* RouterProvider
* Routes and Route
* Nested Routes
* Layouts (RootLayout & ContactLayout)
* Outlet
* Link Navigation
* Dynamic Routing Basics
* Component-based Routing
* Organized Project Structure

## 🛠️ Tech Stack

* React
* React Router DOM
* JavaScript (ES6+)
* Vite
* CSS

## 📂 Project Structure

```text
src
│
├── components
│   ├── Navbar.jsx
│   ├── ContactInfo.jsx
│   └── ContactForm.jsx
│
├── layout
│   ├── RootLayout.jsx
│   └── ContactLayout.jsx
│
├── pages
│   ├── Home.jsx
│   ├── About.jsx
│   ├── Products.jsx
│   └── Contact.jsx
│
├── App.jsx
├── main.jsx
└── index.css
```

## ✨ Features

* Multi-page navigation without page reloads
* Shared layouts using `Outlet`
* Nested routing
* Contact section with child routes
* Clean and modular folder structure
* Beginner-friendly implementation

## 📸 Screens Included

* Home Page
* About Page
* Products Page
* Contact Page
* Contact Info
* Contact Form

> *(Add screenshots here if you want.)*

## ▶️ Getting Started

Clone the repository:

```bash
git clone <repository-url>
```

Navigate to the project:

```bash
cd <project-folder>
```

Install dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

Open your browser and visit:

```text
http://localhost:5173
```

## 📖 What I Learned

* How React Router enables Single Page Applications (SPA)
* Difference between `BrowserRouter` and `RouterProvider`
* Creating routes using `createBrowserRouter`
* Working with nested routes
* Using `Outlet` to render child routes
* Building reusable layouts
* Navigating with `Link`
* Organizing React applications using layouts and pages
* Debugging common React Router errors

## 🎯 Future Improvements

* Dynamic Routes
* Route Parameters (`useParams`)
* Programmatic Navigation (`useNavigate`)
* Loaders and `useLoaderData`
* Error Pages
* Protected Routes
* Authentication

## 👨‍💻 Author

**Hemanth Kumar**

Learning React and the MERN Stack while building practical projects.

